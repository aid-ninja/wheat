## Description

What does this PR do?

## Checklist

- [ ] `npm test` passes (8/8 tests)
- [ ] `node wheat-compiler.js --scan` reports no external dependencies in HTML
- [ ] No new runtime dependencies added to `dependencies`
- [ ] HTML outputs are self-contained (inline CSS/JS, no CDN links)
